<body background="http://thewowstyle.com/wp-content/uploads/2015/04/spring-desktop-backgrounds-hd.jpg">

<center><h1> Add to a To-Do list </h1>

<form action = "/new_todo" method = "post">

User ID:
<select name="user_id"> 
  <% @users.each do |u| %>
    <option value= "<%=u.id%>"><%=u.name%>
    </option>
  <%end%>
</select>

<select name="priority_id"> 
  <% @priorities.each do |p| %>
    <option value= "<%=p.id%>"><%=p.priority%>
    </option>
  <%end%>
</select>

Title:
<input type = "text" name = "title">

Description:
<input type = "text" name = "description">
<input type = "submit">
</form>



<a href="/">Home</a> | 
<a href="/todos/list">To-do list</a> | 
<a href="/logout">Log out</a></center> 
</body>